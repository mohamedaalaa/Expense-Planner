import 'package:flutter/material.dart';
import 'package:planner/models/transactions.dart';
import 'package:planner/widgets/transactions_list.dart';

import 'newTranactions.dart';

class UserTranactions extends StatefulWidget {
  const UserTranactions({Key? key}) : super(key: key);

  @override
  _UserTranactionsState createState() => _UserTranactionsState();
}

class _UserTranactionsState extends State<UserTranactions> {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[


      ],
    );
  }
}
